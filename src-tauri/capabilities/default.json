{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the game window is allowed to do: the core defaults, plus the one weather service it fetches from.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "http:default",
      "allow": [{ "url": "https://aviationweather.gov/*" }]
    }
  ]
}
